# AI Multi-Model Site (Starter)

This is a starter Next.js scaffold for an AI-powered multi-profile site.
Replace environment variables in `.env.local` before running.

## Quick start

1. `npm install`
2. `cp .env.local.example .env.local` and fill env values
3. `npm run dev`

